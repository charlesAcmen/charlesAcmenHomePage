# AGENTS.md

本文件是本仓库所有编码代理的项目契约。开始修改前先读完 `README.md` 和本文件；完成后同步更新已经失真的文档。`CLAUDE.md` 只做 Claude 入口，核心规则以这里为准。

## 产品目标

这是 CharlesAcmen 的个人作品世界，不是一组常规 landing-page section。

访客从海上正面看向 Miami Beach 风格街区，滚轮控制镜头沿中轴线向岸上推进。街区、海滩、车辆、棕榈树和酒店都是白色表面配黑色铅笔线；工作室是画面里唯一具有高饱和迈阿密色彩的空间。它代表 Charles 的个人身份、社交入口和未来作品内容。

任何新设计都必须加强同一个叙事：**线描世界 → 彩色工作室 → 更深的个人内容**。不要为了“丰富”而加入风格独立的卡片区、渐变区、仪表盘或普通作品瀑布流。

## 不可破坏的视觉约束

1. 摄像机从海面正对工作室，主要运动是沿 Z 轴向前推进；不要恢复斜向飞入或大幅旋转。
2. 工作室以外保持纯白表面、深色轮廓和轻微铅笔扰动；避免写实 PBR、复杂纹理和大面积彩色材质。
3. 工作室可以使用珊瑚粉、青绿、黄色、紫色等 Miami / GTA-inspired 色彩，但不得直接复制 GTA 商标、角色、地图、模型或音频。
4. 社交卡片显示用户提供的原图，不额外套厚边框；卡片必须与墙面保留足够距离，形变时不能穿墙。
5. 工作室墙面标识保持 `ACMEN` / `CHARLES`；门、台阶和落日图案当前刻意留空，未经要求不要恢复。当前工作室采用近似正方形的纵深，不要再压回浅长方体。
6. Breakwater Hotel 是手工抽象轮廓，不是下载的精确建筑资产；保持其在线描世界中的低复杂度。

## 当前交互契约

- 滚轮：改变目标进度，镜头阻尼追随。
- 触摸：纵向拖动改变进度。
- 键盘：方向键、Page Up/Down、Home、End。
- 底部 range：直接设置目标进度。
- 车辆：由同一个进度驱动，双向移动并在街道边界循环；hover 抬升，点击触发对应车辆的空间鸣笛。
- 卡片：只在镜头接近工作室后启用 raycast；hover 形变，点击外链或切换 WeChat/QQ 二维码。
- 海浪：五组近岸浪带由同一个浏览进度驱动，推进、抬升、摊开和消失都应保持可逆。
- 声音：意图状态默认开启；浏览器阻止自动播放时，在首次有效用户手势中重试。海浪是全局底声，车辆和喇叭是绑定在车辆节点上的空间声源。
- 显示器：侧墙上的 3D 外壳包围一个 CSS3D YouTube iframe；只在接近工作室后加载，最终阶段才允许指针交互。

## 模块边界

- `app/page.tsx`：HTML/React 页面壳、页头、介绍和索引抽屉。不要把 Three.js 场景代码写回这里。
- `app/coast-scene.tsx`：React 与实时体验之间的薄桥；只管理挂载、进度和声音 UI。
- `app/coast/create-coast-experience.ts`：渲染器、摄像机路径、输入监听、主循环和统一清理。
- `app/coast/model.ts`：只装配世界并转发 update/activate/animate/dispose。
- `app/coast/audio-engine.ts`：所有 Web Audio、媒体元素、声音解锁、混音和暂停逻辑。
- `app/coast/traffic.ts`：车辆形状、布局、进度运动和音频挂点。
- `app/coast/surf.ts`：水面线条和滚轮驱动的拍岸浪带。
- `app/coast/studio-display.ts`：墙挂显示器模型、CSS3D 对象、YouTube 地址和延迟加载门槛。
- `app/coast/social-card-data.ts`：社交平台图片、二维码与 URL 的唯一数据源。
- 其余 `app/coast/*.ts`：按文件名维护环境、建筑、酒店、工作室、卡片和共用资源。

新增功能时优先创建职责单一的小模块，不要继续增长一个几百行的 TSX。React 负责 UI，Three.js 负责画布内世界；不要让两边互相直接操作内部状态。

## 性能规则

1. 不在逐帧循环里无谓创建 `Vector3`、`Color`、数组、几何或材质；临时对象在闭包外复用。
2. 同类物体优先共享几何与材质。大量重复元素再考虑 `InstancedMesh`，不要仅为抽象而过早改写。
3. 保持 DPR 上限；引入阴影、后处理或高分辨率贴图前必须说明 GPU/移动端成本。
4. raycast 只在指针或摄像机发生变化时运行。社交卡片保留接近工作室才启用的门槛；车辆只增加六个共享几何的拾取目标。
5. 页面隐藏时暂停动画和音频；dispose 必须移除事件、DOM 节点、媒体源和 GPU 资源。
6. 长音频继续使用 `HTMLMediaElement` 流式播放。不要改成 `AudioLoader` 后整段解码到内存。
7. 空间音频使用单个摄像机 `AudioListener`；不要给每辆车创建独立 `AudioContext`。
8. 保留 `prefers-reduced-motion` 和移动端输入；新增 UI 必须有键盘焦点与可读标签。
9. CSS3D 与 WebGL 不共享深度缓冲；播放器必须完整收在显示器边框内，不要依赖 3D 几何遮挡 iframe。布局以浏览器 100% 缩放为基准。

## 音频与第三方素材

- 当前素材来源和许可见 `assets/audio/SOURCES.md`，修改素材时同步更新该文件与 README。
- 不下载或提交从 GTA、Rockstar 或其他商业游戏中提取的模型、贴图、音乐、环境声或车辆声。
- “GTA 6 风格”只指 Miami 气质、镜头叙事、饱和配色和开放世界环境感。
- 自动播放可能被浏览器拦截，因此默认开启状态、声音按钮和首次用户手势重试都不可移除。
- 新音频先检查许可、循环接缝、响度、格式支持与文件大小；环境声应平静，不使用惊涛骇浪。
- YouTube 内容使用标准嵌入播放器，不抓取、代理、下载或转码远程视频；更换默认视频只改 `studio-display.ts` 中的 ID。

## 内容位置

- 项目介绍：`app/site-data.ts`
- 社交卡片与链接：`app/coast/social-card-data.ts`
- 图片：`assets/image/`
- 声音：`assets/audio/`
- 页面 metadata：`app/layout.tsx`

当前公开社交入口包括 GitHub、Instagram、YouTube、X、小红书、WeChat 和 QQ。WeChat/QQ 没有外链，必须保留二维码切换行为。

## 修改与验收

至少执行：

```powershell
npx tsc --noEmit
npm run lint
```

涉及构建配置、资源导入或路由时再执行：

```powershell
npm run build
```

不要把源码检查、TypeScript、lint、构建、浏览器加载、交互检查和视觉验收混为同一类证据。报告时明确哪些实际运行过。

已知环境差异：Windows + Node 24 上，Vinext 有时在编译和预渲染结束后的进程清理阶段触发 libuv `UV_HANDLE_CLOSING` 断言。先检查输出和 `dist/client`，但仍应把非零退出如实报告。

站点所有者目前要求只做好本地预览并自行查看。除非被明确要求：

- 不操作浏览器代替用户验收；
- 不部署、不绑定域名、不推送远端；
- 不把“应该能看到/听到”写成已经完成的视觉或听觉验证。

仓库可能包含用户尚未提交的修改。先看 `git status`，保留无关改动，不使用破坏性 git 命令，也不要擅自 commit。
